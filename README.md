# projectgGo
# urlcheck — консольная утилита для массовой проверки URL

**urlcheck** читает список URL из текстового файла, выполняет HTTP GET-запросы с заданным таймаутом, измеряет время до первого байта (TTFB), количество полученных байт, опционально ищет подстроку в теле ответа и выводит результаты в виде красивой таблицы. При необходимости сохраняет результаты в JSON.
## 📦 Установка
### Способ 1: Сборка из исходников
git clone <...>   # или просто скопируйте файл urlcheck.go
cd urlcheck
go build -o urlcheck urlcheck.go

После этого появится исполняемый файл `urlcheck` (или `urlcheck.exe` на Windows).

### Способ 2: Запуск без сборки

go run urlcheck.go [флаги]

## 🚀 Использование

По умолчанию утилита ожидает файл `urls.txt` в текущей папке. Каждый URL — на отдельной строке. Пустые строки игнорируются.

### Базовый запуск

./urlcheck


### Указать свой файл и число воркеров

./urlcheck -file mylist.txt -workers 10


### Поиск подстроки в теле ответа

./urlcheck -contains "Golang"


### Увеличить таймаут запроса

./urlcheck -timeout 30s

### Сохранить результаты в JSON

# JSON в файл results.json (по умолчанию)
./urlcheck --json

# JSON в произвольный файл
./urlcheck --json report.json
./urlcheck --json=report.json


### Комбинация всех флагов

./urlcheck -file urls.txt -workers 5 -timeout 10s -contains "example" --json out.json


---

## ⚙️ Описание флагов

| Флаг                     | Значение по умолчанию | Описание |
|--------------------------|----------------------|----------|
| `-file`                  | `urls.txt`           | Путь к файлу со списком URL (по одному в строке). |
| `-workers`               | `5`                  | Количество одновременно работающих горутин (размер пула). |
| `-timeout`               | `10s`                | Таймаут HTTP-запроса (формат: `5s`, `1m`, `500ms`). |
| `-contains`              | `""` (пусто)         | Подстрока для поиска в теле ответа. Если не указана, поиск не выполняется. |
| `--json` / `--json=FILE` | —                    | Сохранить результаты в JSON. Если имя файла не указано, используется `results.json`. |

**Важно:** флаг `--json` обрабатывается отдельно от стандартного пакета `flag` и поддерживает **опциональное значение**. Это сделано строго по условию задачи.

## 📋 Пример работы

**Содержимое `urls.txt`:**

google.com
httpbin.org/status/404
ya.ru

**Команда:**

./urlcheck -workers 3 -timeout 5s -contains Google --json

**Вывод в консоль:**

URL                                      Статус  OK    TTFB(ms)  Байты  Содержит  Ошибка
-------------------------------------------------------------------------------------------
https://google.com                      200     true   45       12432  true
https://httpbin.org/status/404          404     false  123         0   false
https://ya.ru                           200     true   67       18902  false

JSON сохранён в results.json


## 🧠 Как это работает (кратко)

1. **Чтение файла** – построчно, пропуск пустых строк.
2. **Нормализация URL** – если нет схемы (`http://` или `https://`), автоматически добавляется `https://`.
3. **HTTP-клиент** – создаётся с указанным таймаутом.
4. **Конкурентность** – запускается пул воркеров (флаг `-workers`). Каждый воркер берёт URL из канала, выполняет запрос и отправляет результат в общий канал.
5. **Измерения**:
   - `TTFB` – время от начала запроса до получения заголовков ответа (в миллисекундах).
   - `SizeBytes` – количество байт в теле ответа.
6. **Поиск подстроки** – если задан флаг `-contains`, выполняется `strings.Contains` по телу ответа. Результат сохраняется в поле `Contains` (указатель на `bool`). Если флаг не задан, поле `Contains = nil` (в JSON отсутствует).
7. **Сбор результатов** – главная горутина читает канал результатов и накапливает слайс.
8. **Сортировка** – результаты сортируются по полю `URL` в алфавитном порядке.
9. **Вывод таблицы** – используется `text/tabwriter` для автоматического выравнивания колонок.
10. **Сохранение JSON** – при наличии флага `--json` результаты сериализуются в JSON с отступами и записываются в файл.

## Примечания

- Если при запросе возникает ошибка (нет сети, неверное имя хоста, таймаут), поле `StatusCode` будет равно `0`, `OK = false`, а в колонке «Ошибка» появится текст ошибки.
- В таблице колонка «Содержит» выводит `-`, если поиск не выполнялся (флаг `-contains` не задан).
- JSON-файл содержит все поля структуры `Result`, в том числе `Contains` только если поиск выполнялся (иначе поле отсутствует).
- Код завершается с ненулевым кодом возврата при критических ошибках (не удалось открыть файл, пустой список URL и т.п.).
